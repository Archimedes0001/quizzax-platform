# Production Environment Variables Example
# Copy these to Render's Environment Variables section

# 1. MongoDB Connection String (Get this from MongoDB Atlas)
# Format: mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/quiz_app?retryWrites=true&w=majority
MONGO_URI=your_mongodb_atlas_connection_string_here

# 2. Port (Render will set this automatically, but you can define it)
PORT=5000

# 3. Environment Name
NODE_ENV=production
